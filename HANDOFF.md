> **Continuation:** See [CONTINUATION_2026-09-13.md](CONTINUATION_2026-09-13.md) for the Git repository, subsequent work, validation, and remaining limits. The original handoff below is preserved as historical context.
>
> **Diagram corrections:** See [DIAGRAM_AUDIT_2026-09-13.md](DIAGRAM_AUDIT_2026-09-13.md) for the subsequent visual and semantic audit. Earlier passing layout checks missed internal clipping and misleading physical geometry.
>
> **Further refinement:** See [QUALITY_REFINEMENT_2026-09-13.md](QUALITY_REFINEMENT_2026-09-13.md) for navigation/progress fixes, phone tables, visual polish, and the fresh materials/crystal/wafer diagram review.
>
> **Language and explanation:** See [LANGUAGE_REVIEW_2026-09-13.md](LANGUAGE_REVIEW_2026-09-13.md) for the novice-oriented editorial pass across both tracks, reviewed definition corrections, and the updated source-to-build preservation contract.

# HANDOFF: "Sand to GPU" semiconductor course + interactive site

Written 2026-09-13. Everything lives in this folder (the session's scratch workspace). All background
work has been stopped; nothing is running. The three Workflow runs described below are resumable and
their completed stages are cached.

## 1. What the user wants (in their words, paraphrased)
A very in-depth, accurate course on the entire semiconductor supply chain (quartz → polysilicon → CZ
ingot → wafer → fab → litho/EUV → transistor → interconnect → test → HBM → CoWoS → NVIDIA GPU → rack),
verified by agents, delivered as an interactive teaching website. Iteration 2 asks for:
1. Accuracy: fact-check everything again.
2. Depth: consistent depth across modules ("are we going deep enough, is everything equally deep").
3. Explanation: for a reader who programs and knows school physics/chemistry but no chip specifics;
   the text must take them to expert level, defining everything.
4. The site "well built": they saw visual errors in demos and text.
5. NEW: a shorter, higher-level **survey** version of the course.
6. NEW: more diagrams and interactive elements.
The user hits their spend limit often; they say "continue when it resets, multiple iterations are fine".

## 2. Layout of this folder
```
course/
  CURRICULUM.md, STYLE_GUIDE.md      v1 author brief
  TEACHING_GUIDE.md                  v2 standard: reader, 4 rules, structure, depth rubric  <- READ
  SURVEY_GUIDE.md                    brief for the 10 survey chapters                     <- READ
  REVIEW_BRIEF.md                    fact-check procedure/report format (round 1)
  modules/NN-slug.md                 22 modules. ALL 22 are now v2 rewrites (~18-25k words each,
                                     ~400k total). v1 (fact-checked, ~10k each) is in course/v1/.
  quizzes/NN-slug.json               22 quizzes; 9 have the new 8-question form, 13 are still v1 (6 q)
  review/NN-slug.md                  round-1 fact-check reports (22) + SUMMARY.md
  review/*-r2.md                     round-2 reports: NONE written yet
  survey/                            empty; site expects S01-slug.md ... S10-slug.md
site/
  index.html, app.js, styles.css     the SPA shell (hash routes #/home, #/m/NN, #/s/NN)
  content.js                         GENERATED by `node build.js` from course/  (rebuild after edits)
  widgets/*.js                       46 widget files (28 original + 18 new); see section 4
  widget-placement.json              where each widget is inserted (regex on heading text)
  widget-test.html                   single-widget harness page; lists all 46 scripts
  WIDGET_SPEC.md                     widget contract (registerWidget, ctx.tokens, CSS classes)
qa/
  WIDGET_QA_STANDARD.md              pass criteria for widgets                             <- READ
  NEW_WIDGETS.md                     specs of the 18 new widgets
  render.js                          puppeteer harness (see section 5)
  scenarios/<id>.json                per-widget interaction scripts for the harness
  shots/                             screenshots from previous runs (disposable)
build.js                             markdown -> content.js (LaTeX->Unicode, tilde escaping, TOC, key numbers, survey track)
serve.js                             static server: `node serve.js` -> http://localhost:8790 (PORT env)
.claude/launch.json                  preview config "site" for the Browser pane
README.md                            user-facing readme (partly stale: mentions 28 visuals, 6-q quizzes)
```
Published artifact (hosted copy of the site, private to the user): https://claude.ai/code/artifact/b8b564c2-cd8f-4ba6-bd26-da49d895800d
It currently holds the v1 site. Republish by calling the Artifact tool with `url` = that URL, file_path
site/index.html and the same `files` map (styles.css, app.js, content.js, widgets/*.js). Do NOT pass a
favicon on republish.

## 3. State of the three Workflow runs (resume with Workflow({scriptPath, resumeFromRunId}))
Script files are in
`C:\Users\dicks\.claude\projects\C--Users-dicks-AppData-Roaming-Claude-scratch-workspaces-0825c556-cb7b-4de6-85c1-ff4865168fc7-185ad3f3-7d78-4eb8-a01f-fae74ee79efb-scratch-2026-09-13-9d51cd\a23f2f25-2035-41e8-a17f-3abd88f264e9\workflows\scripts\`
and journals in `...\a23f2f25-2035-41e8-a17f-3abd88f264e9\subagents\workflows\<runId>\journal.jsonl`.
(If those paths are unavailable in the new session, re-author from the descriptions below; the prompts
are also in this session's transcript. Cached results are lost in that case but files on disk stand.)

### A. course-content-v2  (runId wf_e4d87fff-51a, script course-content-v2-wf_e4d87fff-51a.js)
Per module pipeline: audit -> rewrite -> reader test/polish loop (max 2) -> round-2 fact check
(web) -> 2 adversarial refuters x 6 load-bearing claims -> corrector -> quiz (8 q). Then 3 cross-module
consistency agents. Done: 22 audits, 22 rewrites, reader tests for ~20 modules, ~19 polish r1 passes.
NOT done: reader r2 for most, ALL round-2 fact checks, ALL refutation, quizzes for 13 modules
(00-03,05-08,10-12,14-16 still v1 6-q; 04,09,13,17,18,19,20,21 done), all 3 consistency agents.
Resume = re-run only the failed/absent agents. Note the rewritten modules are 18-25k words; the user
likes the depth ("super detailed which is amazing").

### B. site-visual-qa  (runId wf_17c57619-b11, script site-visual-qa-wf_17c57619-b11.js)
Per widget: critic (renders, grades, writes scenario) -> fixer -> verifier loop (max 3). Then shell-qa
agent (index/app/styles/build) and final-sweep agent (renders every page). Done: 28 critiques, 15 fixes
(scale-ladder chain-map deal-grove purity fab-flow upw-mask-cost rayleigh wafer-slicing
euv-stochastics euv-source sadp ald-cycle cz-puller x2), 1 verify (cz-puller failed r1, r2 fix done,
r2 verify not run). NOT done: fixes for etch-profile implant-profile node-table damascene
yield-calculator transistor-evolution test-cost hbm-stack cowos-flow package-xsection gpu-bom geo-share
wafer-price yield-cascade rack-explorer; verifies for almost everything; shell-qa; final-sweep.
The critiques (cached, in the journal) are detailed and good; the chain-map redraw is a model result.

### C. new-widgets-wave2  (runId wf_962a90b4-da5, script new-widgets-wave2-wf_962a90b4-da5.js)
Build -> verify -> fix loop for the 18 widgets in qa/NEW_WIDGETS.md. Done: all 18 built (files exist,
syntax OK), 16 verified: only heat-path PASSED; 15 failed round 1 with issue lists in the journal;
amhs-sim fix r2 done. NOT done: fix rounds for the 15 failures and re-verification; dram-cell and
export-timeline never verified.

## 4. Site integration still to do (manual, ~30 min)
1. Add `<script src="widgets/<id>.js"></script>` lines to site/index.html for the 18 new widgets
   (widget-test.html already lists all 46; index.html lists only the original 28). Keep them before app.js.
2. site/widget-placement.json already has anchors for all 46 widgets; after the rewrites, verify each
   anchor still matches a heading: run `node build.js` then the placement check (a heading regex that
   fails falls back to "after the intro paragraph", which is acceptable but not ideal). Module 19 has
   4 widgets; nvlink-topology anchors "Networking beyond", heat-path anchors "Who builds it".
3. `node build.js` regenerates site/content.js (currently built from the v2 modules, survey empty).
4. Update README.md (46 visuals, 8-question quizzes, survey track) and course/SAND_TO_GPU_full_course.md
   (regenerate by concatenating course/modules/*.md; the script is trivial, see session history).
5. Republish the artifact (section 2) and clear localStorage-test state if you touched the local site.
6. Known shell items not yet verified after my last edits: the Survey/Deep-dive track switch in the
   sidebar, #/s/NN routes, search across both tracks (all tested working with a placeholder chapter);
   `renderProgress` now only touches deep-track sidebar items.

## 5. How to test anything visually (no Browser pane needed)
Start the server: `node serve.js` (port 8790; or `preview_start` name "site"). Then:
```
node qa/render.js widget <id>                       # 760/360 px x dark/light PNGs + JSON report
node qa/render.js widget <id> --scenario qa/scenarios/<id>.json
node qa/render.js page "#/m/08" --width 1400 --theme dark --slices 4
```
PNGs land in qa/shots/ (open them with the Read tool). The JSON report lists console errors, overlapping
SVG text, clipped/overflowing elements, tiny text, hard-coded colors. Puppeteer is installed at the
folder root (node_modules/). Agents running the harness concurrently is fine (each launches its own
headless Chrome); running many concurrent Browser-pane sessions is NOT (they collide).

## 6. Recommended order for the next agent
1. Resume A (content) first: it is the user's top priority (accuracy, depth, explanation). If the
   Workflow cache is unavailable, run per module: reader test -> polish; round-2 fact check with web
   search writing course/review/NN-slug-r2.md; refute top claims; quiz to 8 questions; then the 3
   consistency editors (clusters: 00-04 materials; 00,05-14 fab; 00,15-21 memory/packaging/GPU).
2. Resume B and C (widgets). Quality bar = qa/WIDGET_QA_STANDARD.md; the verifier must try to fail.
3. Write the 10 survey chapters (course/SURVEY_GUIDE.md) from the FINAL modules only; each chapter
   embeds one existing widget via `<div class="widget" data-widget="ID"></div>`; suggested: S01 chain-map,
   S02 cz-puller, S03 fab-anatomy, S04 euv-source, S05 etch-profile, S06 transistor-evolution,
   S07 yield-calculator, S08 hbm-stack, S09 cowos-flow, S10 rack-explorer. Then a light fact-check that
   every number in a chapter appears in a deep module.
4. Integration (section 4), a final full-site sweep (render every #/m and #/s page at 1280 and 375,
   both themes, zero widget errors, zero horizontal overflow), republish, and report to the user with
   the verdict table (reports in course/review/).

## 7. Gotchas learned the hard way
- Single "~" is GFM strikethrough; build.js escapes it outside code blocks. Authors use "~" everywhere.
- Inline LaTeX ($...$) is converted by build.js (convertMath); money "$" spans are left alone.
- registerWidget must exist before widget scripts load (index.html defines window.__WIDGETS first).
- HTML entities in TOC/search/key-number rail are decoded in build.js/app.js (was a visible bug).
- The Artifact viewer runs the page in a cross-origin iframe: you cannot script it; test locally.
- Node `String.replace` with a replacement containing `$$` collapses to `$` (broke `$$(` selectors once).
- Spend-limit failures return null from agent(); the pipelines already `.filter(Boolean)`/guard nulls.
- Widget builders may edit site/widget-test.html concurrently (they append their script line); never
  let them touch index.html.
- Memory notes for this project are in the session memory dir (semiconductor-course-project,
  course-v2-iteration, sand-to-gpu-artifact).
